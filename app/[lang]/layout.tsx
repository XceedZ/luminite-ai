// Deprecated legacy layout kept temporarily to avoid build errors if referenced.
// This file is no longer used after removing locale-prefixed routes.

// Tipe `params` sudah benar sebagai Promise
export default function UnusedLocaleLayout({ children }: { children: React.ReactNode }) {
  return children;
}
